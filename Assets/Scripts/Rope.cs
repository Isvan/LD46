using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rope : Pickup
{
    public GameObject attachedObject;

    // Start is called before the first frame update
    void Start()
    {
        attachedObject = null;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
